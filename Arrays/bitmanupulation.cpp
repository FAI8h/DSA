#include <iostream>
#include <vector>
#include <algorithm>

using namespace  std;

void clearBits(int num, int from, int to){
    int st = from;
    int end = to;

    int numberMask;
    for (int i = st; i < end; i++){
        // numberMask = 
    }
}

int main () {
    // clearBits(15, 1, 3);
    cout << (4 % 5) << endl;

    return 0;
}